# TODO

## Packaging / bootstrap

- [x] Treat the repo root as the canonical skill package
- [x] Externalize default runtime state to `.MAL-Updater/`
- [x] Add `bootstrap-audit` as the first install/onboarding readiness check
- [x] Replace committed absolute-path systemd units with install-time rendered templates
- [x] Replace timer-first unattended automation with a user-level daemon-first model
- [x] Establish public-repo anonymization as an explicit project constraint for tracked code/references/tests/history
- [ ] Keep tightening the bootstrap/onboarding UX for new OpenClaw installs (`bootstrap-audit` now also reports runtime-init readiness, daemon install/drift readiness, secrets-dir permission posture, explicit manual-vs-daemon operation expectations, provider-intent/partial-bootstrap counts, and per-provider operation-mode guidance/next-command hints for staged providers; it now also downgrades staged providers from `ready-for-unattended` when session residue or repeated unattended auth-style failures already imply auth degradation, and now applies the same conservative posture to MAL OAuth so repeated unattended `mal_refresh` failures point operators back to `mal-auth-login` instead of looking bootstrap-ready just because token files exist; it can recommend `init` / MAL re-auth / provider re-bootstrap / `scripts/install_user_systemd_units.sh` / `chmod 700` explicitly, those onboarding/recommended-command entries now preserve stable `reason_code` / automation-safety / auth-remediation metadata, MAL/provider operation-guidance lanes now preserve matching per-lane next-command metadata instead of exposing only a bare command string, terse `--summary` output now also exposes the top recommended command plus its reason-code / automation-safe / auth-interaction posture alongside auth-classification lines, and now emits that same metadata for MAL/provider next-command hints too, and the JSON payload now mirrors that prioritization through top-level `recommended_command` / `recommended_automation_command` fields; next likely step is deciding how much broader provider-specific health remediation detail belongs in bootstrap surfaces vs remaining health-check-only as more source providers exist)
- [x] Make repo-root test execution work without manual `PYTHONPATH` fiddling so future development turns can validate faster with plain `pytest -q`
- [x] Make bootstrap/install metadata more machine-readable with provider readiness and explicit recommended commands in `bootstrap-audit`

## Daemon / operations

- [ ] Tighten daemon control loops and per-lane state tracking (cadence / decision timing / last-run start-finish-duration / next-due / active-backoff observability now persists in service state, including warn-vs-critical budget backoff plus adaptive failure-aware provider cooldown state; generic source-provider budget defaults now exist so new providers no longer inherit Crunchyroll implicitly, task-level budget override tables now exist for lane-specific limit/floor policy, the repo now ships built-in `sync_apply` task defaults for MAL hourly limit / projected request cost / learned-history depth / conservative learned percentile / cooldown floors plus bounded unattended execution posture via `service.task_execute_limits`, budget gating can now use per-lane projected request cost from explicit config, fetch-mode-specific task defaults, or observed history, learned projections are now tunable per task and per provider via observed-history windows plus optional percentile baselines, burstier lanes still auto-upshift to a conservative learned p90 when recent request history is clearly spiky, execution-signature changes now clear stale projected/backoff residue for lanes whose unattended posture materially changed, the repo now ships opinionated Crunchyroll/HIDIVE provider defaults instead of leaving that entirely to examples, Crunchyroll/HIDIVE incremental + full-refresh cold starts now also have shipped mode-specific projected-request defaults, HIDIVE now also ships a built-in learned p90 percentile baseline instead of relying only on hard-coded request-count seeds, built-in projected-request defaults now behave as cold-start seeds across MAL refresh / apply / full-refresh lanes instead of pinning those lanes to synthetic costs forever, unattended `mal_refresh` now also ships a small explicit cold-start request-cost seed plus a shallow learned-history window so fresh installs do not treat token refresh as a zero-cost mystery lane, overdue full refreshes now degrade gracefully to incremental fetches when only the heavier mode is over budget, failed provider full-refresh attempts no longer falsely advance the success markers/anchor, and `service-status` / `service-status --format summary` now also compute each provider fetch lane's currently planned fetch mode plus any current full-refresh reason so overdue/deferred resweeps are visible without reading raw state or waiting for the next daemon pass; next likely step is deciding which future source lanes deserve shipped learned-percentile posture and whether HIDIVE needs a different percentile/window than the current conservative p90 default)
- [ ] Continue refining request-budget accounting / backoff behavior for MAL and source providers (warn-threshold pacing + recovery-window backoff now exist, plus provider-specific cooldown floors, generic source-provider default hourly/backoff/auth-failure floors, auth-style failure-aware cooldown classification/floors after provider task errors, and health-check auth rebootstrap recommendations after repeated auth-style provider failures; that operator guidance now covers the MAL token-refresh lane too, so repeated unattended MAL `invalid_grant` / refresh-material failures point operators back to `mal-auth-login` instead of looking like generic daemon residue; auth-fragility classification now also catches refresh/login residue, missing-refresh-token / missing-refresh-material residue, malformed-token-payload residue, and provider session-state `auth_failed` phases via a shared detector used by both daemon/runtime and health-check surfaces, bootstrap/health details now expose those classified residue buckets explicitly so operators can see *why* reauth is being recommended, and maintenance/bootstrap guidance now also preserves residue-specific remediation metadata/reason-codes even when the same auth-login command is shared by multiple failure shapes; next likely step is deciding whether any auth residue now justifies a truly distinct operator command/workflow beyond the current shared auth-login/rebootstrap surface)
- [ ] Decide whether to fully retire the remaining transitional wrapper scripts after more operators move to repo-native cycle commands (`health-check` now has a repo-native `health-check-cycle` CLI path, `exact-approved-sync-cycle` now exists as the repo-native guarded fetch+apply path, `health-check-cycle` now honors the same review/backlog/coverage tuning knobs as `health-check`, bootstrap-audit no longer treats external `flock` as a readiness blocker for the repo-native daemon/runtime flow, and both shell scripts remain only as compatibility shims)
- [x] Add richer service-state/observability surfaces for debugging unattended failures
- [x] Add `service-status --format summary` as a terse/operator-summary mode alongside the rich JSON surface

## Product / sync quality

- [ ] Continue reducing genuinely ambiguous mapping residue (the last live Haruhi-style 14+14 same-title split bundle now auto-resolves under heuristics revision `2026-03-22a`, supplemental-rescued exact-title overflow cases such as `Girls Bravo` now also surface conservative bundle companions for review without widening auto-approval scope, alias-driven exact-title overflow can now surface review companions when franchise alignment only shows up through MAL alternative titles, numbered bundle-companion selection now prefers contiguous follow-up installments over skip-ahead season noise when the episode-fit envelope ties, standalone installment-only provider season labels such as bare `III` / `2` now prefix the base title and count as real season cues even when provider metadata omits `season_number`, explicit later-season searches can now also try small franchise-specific sequel aliases such as `Railgun S` when MAL does not file the sequel under a generic `Season N` label, those generic season-query + franchise-specific sequel-alias paths now also reuse season numbers inferred from provider season titles when provider metadata omits `season_number`, relation expansion now also kicks in for explicit later-season base-series false positives so hidden sequel nodes reachable only through MAL relations can still be recovered conservatively, exact classification now also treats those inferred provider season numbers as real later-season context so sparse provider metadata does not demote otherwise clean explicit sequel resolutions back to review-only posture, the same franchise-specific alias table now also contributes season-context inference when the provider season title itself is alias-labeled (for example `Demon Lord, Retry! R`) so exact alias hits no longer lose later-season confidence just because numeric metadata is absent, alias-query season evidence now only applies when the candidate itself actually matches that sequel alias so base-title false positives returned under alias searches can still fall through to relation-recovered sequel nodes instead of inheriting fake later-season confidence, and whole-season later-season labels now penalize split-part/cour candidates when provider episode evidence already fits a non-segmented season so a plain `Season 2` / `II` title does not tie with `Part 2`; next residue should be the remaining weaker franchise-specific sequel-label ambiguity beyond the now-covered alias-query + alias-title inference + relation-recovery cases)
- [ ] Continue stabilizing fresh Crunchyroll fetches on auth-fragile hosts (daemon-side periodic provider full-refresh cadence now exists via `service.full_refresh_every_seconds`, the daemon now honors health-check `refresh_full_snapshot` recommendations when partial-coverage warnings persist, and health-check can now recommend provider auth rebootstrap after repeated auth-style daemon failures even when provider state is already partially missing; next likely step is smarter auth-fragility classification/escalation for less-obvious provider-specific failure modes beyond the newly covered missing-token / malformed-token residue)
- [ ] Keep improving recommendation quality and review UX (recommendation/recommendation-metadata paths are now provider-aware across Crunchyroll + HIDIVE mappings, including relation-backed continue-next and discovery-seed refresh flows; grouped recommendation output now surfaces effective provider availability via section/item labels and counts, merged cross-provider items now correctly mark their sections as mixed-provider instead of looking single-provider, both grouped + global top-N ranking mildly prefer broader availability when priorities tie so `--limit` is less likely to hide a multi-provider equivalent, merged cross-provider continue/episode items now also receive a small raw-priority bonus with explicit availability reason/context metadata so broader availability can matter slightly before the tie-break without dominating the core score, discovery candidates now also expose support-balance evidence (`best_single_source_votes`, `cross_seed_support_votes`, `effective_best_single_source_votes`, `effective_cross_seed_support_votes`, `support_balance_bonus`) so bursty one-seed MAL edges lose to steadier cross-seed consensus when aggregate support is otherwise close, and that support-spread bonus now follows effective positive/current support after neutral/stale weighting instead of raw vote spread alone; discovery ranking now also includes modest freshness / seed-activity weighting (`start_season`, `freshness_bucket`, `freshness_bonus`, `freshness_penalty`, `catalog_age_in_seasons`, `recent_seed_activity_bonus`, `freshest_supporting_seed_days`) so tied candidates lean toward newer catalog entries, very old catalog entries can take a small explainable age decay, and recently active taste signals still matter instead of aggregate votes alone, materially old supporting seeds now also get a small explainable stale-support decay (`stale_supporting_seed_ids`, `stale_support_ratio`, `stale_support_penalty`) so old taste evidence no longer reads like equally current consensus, stale-heavy multi-seed candidates now also expose `base_effective_supporting_seed_count` plus `stale_consensus_discount` so older seed activity can reduce the consensus-count boost itself instead of only taking a late penalty, near-ties now also respect small explainable seed-quality signals (`seed_quality_bonus`, `supporting_seed_scores`, `best_supporting_seed_score`) so highly scored / more deeply completed seed titles count a bit more than weak support without overwhelming aggregate votes, discovery scoring now also carries a symmetric low-confidence penalty (`seed_quality_penalty`, `penalized_seed_scores`, `lowest_supporting_seed_score`) so candidates backed mainly by poorly scored/disliked seeds get tempered slightly instead of looking like equally strong taste matches, discovery now suppresses candidates backed only by explicitly dropped or disliked-only seeds instead of merely showing a warning-shaped penalty, mixed-signal candidates now also discount negative supporting seeds from their support-count boost while surfacing `negative_supporting_seed_ids`, `negative_support_ratio`, `effective_supporting_seed_count`, and `mixed_signal_penalty`, explicitly neutral MAL seed scores now also count more conservatively via `neutral_supporting_seed_ids`, `neutral_support_ratio`, and `neutral_support_penalty` so neutral-heavy support ranks below cleaner positive consensus, and metadata-rich near-ties now also get a small `metadata_affinity_bonus` / `metadata_match_dimensions` bump when cached genre/studio/source overlap aligns across multiple seed dimensions at once, with a mild extra preference for broadly popular candidates inside that same metadata-rich bucket; review-queue/health triage surfaces now also resolve backlog title labels from the matching provider instead of assuming Crunchyroll-only series rows, and `list-mappings` now defaults to all providers with an explicit `--provider` filter instead of silently showing Crunchyroll only, so the next likely step is deciding whether recommendation ranking now needs richer popularity/mean calibration inside metadata-rich ties or whether recommendation work should rotate back toward daemon/bootstrap lanes again)
- [ ] Keep the upstream GitHub issue tracker active as the canonical channel for third-party bug reports and feature requests
