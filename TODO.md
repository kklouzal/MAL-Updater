# TODO

## Immediate next build steps
- [x] Create Python project scaffold
- [x] Define normalized snapshot contract
- [x] Define SQLite schema
- [x] Decide config/secrets directory layout
- [x] Wire Python config loader to parse `config/settings.toml`
- [x] Add JSON schema validation before ingestion
- [x] Add Python ingestion path for snapshot -> SQLite
- [x] Verify a real Crunchyroll credential login + refresh-token bootstrap on this machine with staged credentials
- [x] Get the first real live Crunchyroll snapshot on this machine
- [x] Keep the working live Crunchyroll path in Python (`curl_cffi` transport when available)
- [x] Implement local MAL loopback OAuth flow + token persistence
- [x] Register MAL app / complete live OAuth with real credentials

## Sync policy
- [x] Add durable approved-mapping persistence in `mal_series_mapping`
- [x] Add operator review workflow for mapping approval / preservation
- [x] Add approved-mappings-only dry-run gate for the future live executor
- [x] Implement evidence-based credits-skipped completion handling (strict ratio + remaining-time window + later-episode evidence)
- [x] Implement missing-data-only merge rules
- [x] Implement conflict / ambiguity review queue generation from mapping/sync passes
- [x] Replace ad-hoc review output with persisted `review_queue` entries once the review shape settled enough for first use
- [x] Build the first guarded live MAL apply path for approved forward-safe proposals

## Recommendations
- [x] New dubbed episode alerts
- [x] New season alerts for completed shows
- [x] Keep later-season installment heuristics conservative (`Part N` only counts with explicit season-style wording)
- [x] Suppress obvious skipped-episode / fully-watched false positives in local episode alerts
- [x] Add recency-aware ranking bias to local episode alerts
- [x] Split stale tail gaps into `resume_backlog` instead of labeling them as fresh episode alerts
- [x] Tighten the `fresh_dubbed_episodes` window so 3-4 week old tail gaps stop crowding the fresh-alert lane
- [x] Add a local MAL metadata/relation cache and refresh command for richer continuation evidence
- [x] Add a first graph-backed `discovery_candidate` recommendation lane from cached MAL recommendation edges
- [x] Group recommendation output into clearer category sections instead of a single flat list
- [~] Genre/studio/source/VA/taste feature ideas (started with cached genre+studio+source overlap bias for discovery candidates plus suppression of direct-franchise and already-in-provider-catalog rediscovery; VA/taste-model work still open)

## Later

### Current round-robin target order
Automation/maintenance is now baseline, not the active development lane: the health-check surface, repo-owned user-systemd installer/units, and wrapper scripts are in place and validated against the current full stdlib suite (`221` tests green on 2026-03-20). Future passes should only come back here for regressions or narrowly scoped polish.
Current live maintenance residue after the latest mapper/health pass: `health-check --format summary` reports `mapping_review_stale_heuristics=30/30` and recommends `review-queue-refresh-worklist --limit 3 --per-bucket-limit 20 --mapping-limit 5`; treat that as bounded queue cleanup, not as a reason to steal the next main development slot from the fetch/mapping product lanes.
- 1. Re-stabilize fresh live Crunchyroll fetches after the current `watch-history` HTTP 401 regression; this is the highest-impact upstream blocker because every downstream mapping/sync pass benefits from a truly fresh dataset again.
- 2. Decompose combined Crunchyroll entries more honestly so season/movie matching stops over-trusting aggregate episode counts.
- 3. Reduce the remaining same-franchise low-margin residue without weakening explainability.
- 4. Revisit whether the final OpenClaw-facing automation surface should stay a thin skill wrapper or grow a repo-local command layer only after the fetch/mapping product lanes move again.

- [ ] Re-stabilize fresh live Crunchyroll fetches after the current `watch-history` HTTP 401 regression (credential re-login succeeds, but full snapshot fetch still depends on `watch-history` surviving at least until the incremental overlap boundary is reached)
- [x] Add a conservative incremental Crunchyroll sync boundary/checkpoint so repeated runs can stop history/watchlist paging once already-synced overlap is detected
- [x] Build OpenClaw skill wrapper (done: the in-repo `skills/mal-updater/` wrapper exists with validated packaging and repo-local CLI recipes, and a workspace-local `~/.openclaw/workspace/skills/mal-updater/` copy now exposes the same thin operator-facing surface directly inside OpenClaw without duplicating business logic outside the Python CLI)
- [x] Add one-shot queue triage helpers for the remaining ambiguous mapping buckets once enough real review residue patterns are captured (summary now clusters repeated title/franchise buckets, repeated fix-strategy signatures, repeated normalized family buckets, and combined franchise+fix-strategy buckets, `list-review-queue` can drill directly into a chosen `--title-cluster`, `--decision`, `--reason`, `--reason-family`, `--fix-strategy`, `--fix-strategy-family`, `--cluster-strategy`, or `--cluster-strategy-family`, summary payloads now ship exact `drilldown_args` plus status-aware queue action commands that preserve active scope filters including non-default `--status`, `review-queue-next` / `review-queue-worklist` now auto-rank the broader family buckets before narrower exact slices when that batches more residue, mapping-review buckets also surface bounded `refresh-mapping-review-queue` commands and that refresh command can now consume the same queue-slice filters directly instead of only raw `--provider-series-id` lists, `resolve-review-queue` can mark filtered open residue rows resolved from the CLI with a bounded `--limit`, `reopen-review-queue` can move filtered resolved rows back to `open` when a cleanup pass was too aggressive, `review-queue-apply-worklist` now applies the top ranked buckets in one bounded bulk pass with overlap dedupe across both family and exact slices, and `review-queue-refresh-worklist` can bulk-refresh the highest-signal stale mapping buckets in one bounded pass instead of forcing one targeted refresh command at a time)
- [x] Periodic health-check/maintenance loop (done: first local `health-check` CLI now reports auth material presence, latest ingest freshness, provider counts, mapping counts, and open review backlog with a configurable stale-snapshot threshold; it includes actionable `recommended_next` / `recommended_worklist` queue triage hints when backlog exists, now also emits matching `recommended_apply_worklist` and `recommended_refresh_worklist` batch commands for the bulk resolve/reopen and stale-residue refresh helpers, those recommendations can be focused to `mapping_review` or `sync_review` via `--review-issue-type` with configurable worklist depth, it now warns when approved mapping coverage falls below `--mapping-coverage-threshold` so an empty review queue cannot masquerade as a fully healthy state, and it now also emits `maintenance.recommended_commands` when auth state is present but the repo still needs an ingest refresh step or a mapping-review backlog rebuild. `maintenance.recommended_command` / `maintenance.recommended_automation_command` now preselect the top overall repair versus the first safely automatable one, the wrapper logs when a safe candidate was skipped only because the allowlist blocked it, and `health-check` now also reports whether the repo-owned user systemd units are actually installed for the current user while recommending `scripts/install_user_systemd_units.sh` when that automation layer is still only documented rather than installed. Installed repo-owned user units are now also compared against the checked-in versions so health-check can catch stale service/timer drift instead of only missing files, and it now also detects the next false-green cases where the unit files exist but the timers were never enabled, the timer symlink points at the wrong target, or the timer is enabled on disk but not actually active in the live user systemd runtime. Missing, outdated, disabled, miswired, or runtime-inactive repo-owned automation is prioritized ahead of lower-priority mapping-backlog rebuild suggestions so unattended automation repair stops getting buried under secondary cleanup work. Repo-owned `scripts/run_health_check_cycle.sh` + user systemd health-check units now provide a recurring local wrapper/logging surface, `scripts/install_user_systemd_units.sh` now installs/updates the checked-in user units idempotently and reports exactly which unit files changed (plus whether the optional health env file was installed/preserved/skipped), `health-check --strict` can now fail fast for automation without dropping the JSON payload, `health-check --format summary` now exposes the same high-signal maintenance view as terse operator-facing lines and now also prints direct automation install/update hints plus live timer runtime/next-trigger state when available, the health output now explicitly warns when the latest incremental ingest only refreshed a subset of cached provider rows and recommends a `--full-refresh` repair command instead of falsely reporting a fully fresh local cache, that stronger full-refresh repair now takes precedence over the weaker incremental refresh recommendation so optional auto-remediation does not waste a cycle, maintenance recommendations now explicitly declare whether they are automation-safe or require interactive auth so wrappers do not blindly run the wrong command class, low-coverage mapping maintenance now defaults to a bounded runnable `review-mappings --limit N --persist-review-queue` recommendation (`--maintenance-review-limit`, default `25`) so maintenance passes leave durable residue behind instead of only printing ephemeral review output, targeted `refresh-mapping-review-queue --provider-series-id ...` refreshes now let operators re-evaluate just the stale persisted residue touched by newer mapper heuristics instead of blowing away the whole queue, open mapping-review rows now carry a `mapper_revision` stamp and `health-check` warns when backlog was produced by older/unknown heuristics so stale residue can be refreshed before humans spend time triaging it, health-check now prefers the batch `review-queue-refresh-worklist` maintenance command whenever multiple mapping-review slices are open, the timer runtime probe now parses `systemctl show` output by `KEY=VALUE` field name instead of trusting returned line order so healthy enabled timers stop generating false inactive warnings on hosts that reorder properties, and the standalone wrapper now mirrors the CLI summary more faithfully by surfacing `maintenance_recommended_auto_command` plus shell-quoted auto-remediation command logging while correctly executing direct repo-owned shell recommendations like `scripts/install_user_systemd_units.sh` when they are explicitly allowlisted; validated against the current full stdlib regression suite before being treated as the maintained baseline)
- [ ] Decide whether the final OpenClaw-facing automation surface should stay as a thin skill wrapper over the existing CLI intents or grow a more opinionated repo-local command layer
- [ ] Reduce the remaining same-franchise low-margin residue without weakening explainability (today: stronger single-episode special/OVA discrimination landed, exact-title base-series TV matches now auto-promote when the only near runner-up is explainably weaker one-off OVA/auxiliary noise, exact-title TV matches now also auto-promote over non-exact OVA/ONA sidecars when the runner-up is only a franchise extension rather than an exact title, non-exact same-franchise TV sequel suffix variants like trailing `S` / `T` / numeric sequel tags now also stop blocking exact base-series auto-approval when they are just extension noise rather than a true competing exact match, near-identical franchise-extension suffix variants like `TV Specials` / trailing sequel tags such as `R` now also get downgraded when the provider only names the base series, alpha-digit spacing mismatches like `PERSONA5` vs `Persona 5` no longer create false near-ties, lone stylized single-letter roman tokens like `X` are no longer misread as installment hints so titles such as `Tales of Zestiria the X` stop picking up fake `roman:10` evidence, relation-assisted expansion now also recovers hidden base-series matches from suffix/auxiliary residue like `Shuffle! Memories` without re-triggering on plain `Season 1` TV labels, explicit provider season context now also pushes non-TV sidecars/PVs back down when relation expansion surfaces them against a season-labeled mainline candidate, and split-specific season matches can now auto-promote over otherwise tied broader same-season TV candidates when only the top result carries the provider’s part/cour-style evidence; later-season/base-installment collisions now get an explicit base-installment penalty, but harder canonical-entry/side-story cases still remain)
- [ ] Decompose combined Crunchyroll entries before treating aggregate episode counts as hard blockers for season/movie matching (today: aligned later-season matches can downgrade raw max-episode conflicts into `aggregated_episode_numbering_suspected` instead of a full blocker, exact-title overflow cases that appear to span a small MAL two- or three-entry bundle now surface explicit `multi_entry_bundle_suspected` rationale in review residue, later-season installment winners can now also carry that same bundle suspicion when their episode evidence obviously spills into the next season-sized MAL entry, lower-scoring same-franchise later-season companions can still be recovered when they share the same title family plus installment evidence, higher-scoring OVA/recap sidecar noise no longer displaces those real TV bundle companions, exact-title one-shot movie/prologue sidecars now also get demoted when the provider evidence clearly describes a multi-episode row, and mapping-review / CLI output now carries both the strongest suspected companion candidate plus the full suspected companion set so operators can see the likely bundle shape without re-deriving it from the runner-up list)
- [ ] Decide whether episode-title matching can be added from a trustworthy metadata source without pulling the project into brittle scraping or confidence theater

- [x] Add conservative Crunchyroll request spacing to the live fetch path
- [x] Add an exact-approved-only executor gate for unattended runs
- [x] Add a lock-protected exact-approved sync wrapper + checked-in user systemd timer units for a jittered ~8-hour-average local cadence
