name: release
on:
  push:
    tags: ['v[0-9]+.[0-9]+.[0-9]+']
permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write
jobs:
  quality:
    uses: ./.github/workflows/quality.yml
  release:
    needs: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with: {python-version: '3.13'}
      - run: pip install build && python -m build
      - run: python scripts/check_distribution.py dist
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: {registry: ghcr.io, username: '${{ github.actor }}', password: '${{ secrets.GITHUB_TOKEN }}'}
      - id: meta
        uses: docker/metadata-action@v5
        with: {images: 'ghcr.io/${{ github.repository }}', tags: 'type=semver,pattern={{version}}'}
      - id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: '${{ steps.meta.outputs.tags }}'
          labels: '${{ steps.meta.outputs.labels }}'
          provenance: mode=max
          sbom: true
      - uses: sigstore/cosign-installer@v3
      - run: cosign sign --yes "ghcr.io/${GITHUB_REPOSITORY,,}@${{ steps.build.outputs.digest }}"
      - run: |
          mkdir -p release-bundle/docs
          cp compose.yaml .env.example README.md CHANGELOG.md RELEASE_NOTES.md LICENSE release-bundle/
          printf '%s\n' "${GITHUB_REF_NAME#v}" > release-bundle/VERSION
          cp references/CONTAINERS.md references/OPERATIONS.md release-bundle/docs/
          cp dist/* release-bundle/
          (cd release-bundle && find . -type f ! -name SHA256SUMS -print0 | sort -z | xargs -0 sha256sum > SHA256SUMS)
          tar -czf "mal-updater-${GITHUB_REF_NAME}.tar.gz" release-bundle
          (cd dist && sha256sum *) > SHA256SUMS
          sha256sum "mal-updater-${GITHUB_REF_NAME}.tar.gz" >> SHA256SUMS
      - uses: actions/attest-build-provenance@v2
        with:
          subject-path: 'mal-updater-*.tar.gz'
      - uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          files: |
            dist/*
            mal-updater-*.tar.gz
            SHA256SUMS
